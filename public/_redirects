# SPA fallback — serve index.html for all non-asset routes.
# API proxying is handled by Cloudflare Pages Functions in /functions/,
# not here — _redirects cannot proxy to external origins.
/*  /index.html  200
